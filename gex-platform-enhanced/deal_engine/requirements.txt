# Sprint 1 — minimum to run the auth bridge + stub endpoints.
# Sprint 2 will add: numpy, scipy, pandas.

fastapi==0.115.0
uvicorn[standard]==0.32.0
pydantic==2.9.2

# JWT verification
PyJWT[crypto]==2.9.0   # `crypto` extra pulls in cryptography for ES256/RS256
httpx==0.27.2          # JWKS fetch

# Logging niceties (optional)
python-json-logger==2.0.7
