venv/
.venv/
micro_service/
__pycache__/
*.py[cod]
*.egg-info/
node_modules/
*.db
*.sqlite
.env
.env.local
.DS_Store
*.pages
*.pdf
.idea/
.vscode/
~$*

# Docker build staging — regenerated by sync-to-docker.sh, never commit
docker/gex-platform-enhanced/
docker/gex_pf_engine/
docker/gex_fuel_catalog.json
branch_structure.json
temp_auto_push.bat
temp_interactive_push.bat

# Local Claude Code permissions — machine paths, personal to one developer
**/.claude/settings.local.json

# Rename/backup artefacts, never part of a release
.backups/
.backups-*

# User content, never source. Added 2026-09-20 with the canvas cutover:
# these are customers' plant designs and evidence documents, which do not
# belong in a code repository at all — least of all one that was public.
gex-platform-enhanced/backend/data/canvas_blobs/
gex-platform-enhanced/backend/data/package_docs/

# Operational artefacts from a specific migration run — not source, not a release
backend/db_backups/
